eureka:
  client:
    fetch-registry: false
    register-with-eureka: false

logging:
  level:
    web: debug

resilience4j:
  circuitbreaker:
    instances:
      driver:
        waitDurationInOpenState: 15s